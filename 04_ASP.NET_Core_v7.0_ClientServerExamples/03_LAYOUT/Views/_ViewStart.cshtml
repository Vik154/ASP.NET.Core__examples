@*
    _ViewStart - срабатывает перед каждым файлом который есть в представлении
    Благодаря этой настройке все файлы компоновки буду применять
    эту настройку по умолчанию, если не указано обратное,
    при этом если в Layout записать другой параметр тогда он и будет применен
*@
@{
    Layout = "_Layout";
}
